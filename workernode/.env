# -- Redis Configuration --
# (Ensure this matches your running Redis instance)
REDIS_URL="redis://127.0.0.1:6379/0" # /0 selects database 0

# -- Celery / BullMQ Queue Configuration --
# (MUST match the queue name used by your Node.js API to *add* jobs)
SCRAPE_QUEUE_NAME="media-scrape-jobs"

# -- Worker Configuration --
# (Number of browser instances to run concurrently - START LOW!)
WORKER_CONCURRENCY=1

# -- Scraper Configuration --
TARGET_BASE_URL="https://ww19.0123movie.net/" # !!! REPLACE - Base URL of the site !!!
HEADLESS_MODE="false" # 'true' for server, 'false' for visual debugging locally
BROWSER_TIMEOUT="90000" # Timeout for browser operations (milliseconds)

# -- Proxy Configuration (Optional - Enable and fill if using Evomi/Other) --
PROXY_ENABLED="false" # Set to 'true' to enable proxy use

PROXY_USER="supremexre5"
PROXY_PASS="FHkeY8riqA9nxNYWJ0iM_country-US_region-new.york_city-queens_session-KTTG6OSOC_lifetime-240_adblock-1"
PROXY_HOST="core-residential.evomi.com"
PROXY_PORT="1000"
PROXY_TYPE="http" # Or "https" if required by Evomi for this connection type